# Repair checkpoint: release blocked

The candidate contains the semantic, batch, and bounded appearance repairs. It is **not deployed**. The existing production bundle remains `index-D2xOPPSe.js`.

## Decision

The final repaired local guard permits 34/40 frozen held-out bold labels (85%). The release criterion requires at least 38/40 final Matches in each of three uncached runs. Because local evidence is mandatory, even perfect cloud responses cannot pass this gate. The old guard permits 30/40. Five repaired failures lack a confidently recognized heading; one has a stem ratio of 0.1667, below the unchanged 0.17 threshold.

Known fresh bold fixtures improve from 5/8 to 7/8 locally. Arial 28 and Trebuchet 28 recover. Georgia 28 still has merged components and remains unresolved. All eight paired regular fixtures remain locally vetoed. Both regular Century fixtures, including the family that fooled both models, remain vetoed (ratio 0.16). One held-out regular Gill Sans fixture passes the local guard; its final cloud outcome is unmeasured. Local guard counts are not model classification accuracy.

Per the approved bounded decision, no new model search, threshold relaxation, preview, Worker deployment, source publication, or production promotion occurred. The three cloud runs, full 300-image browser rerun, and accuracy-qualified latency gate remain pending. They were not replaced by the unit suite or the fast sample result.

## Completed changes and evidence

- Independently extract complete type, production-role/address, explicit origin, and alcohol declarations. Preserve case tolerance, numeric quantity/proof checks, and ambiguous Review states. Partial geometry cannot hide conflicting text. Role-owned text cannot establish a prominent brand.
- Reconcile CSV rows and images in both directions before per-image OCR/inference. Exact filenames and duplicate rejection remain. A 300-entry reversed-order unit case proves filename association, not a 300-image runtime result.
- Add a crop-local connected-component fallback for the fifth capital in WARNING. Reject merged/ambiguous glyphs. Keep both fixed models and the 0.17 threshold. Skip cloud when the local guard vetoes; cache only successful corroborated responses within one run, applying the local guard every time.
- Clean install (`npm ci --ignore-scripts --offline`), 64 unit tests, production build, and 22 full-label OCR field cases pass. All named valid image cases in that repair suite remain Match; decoys/conflicts never Match.
- Actual local browser plus real cloud service: sample all applicable checks Match in 2.0 s; malformed CSV blocked; a mixed batch retained two reviews and one corrupt-image failure; stop retained one review and two remaining; restart processed all three anew. One restarted sample hit the deadline at 4.8 s, stayed Review, and human confirmation did not upgrade it. No p95 claim is made.
- Editing inputs clears findings. Narrow layout at 390 px has no horizontal overflow; keyboard Tab moves from brand to class/type.
- Full-runtime autoreview accepted and fixed three findings, then returned clean. Command: `/Users/HQ/.agents/skills/autoreview/scripts/autoreview --mode commit --commit HEAD` with the full-runtime context prompt. Runtime files were reviewed as a synthetic snapshot commit, not as actual project history. See `repair-autoreview.md` and runtime hashes in `repair-validation.json`.

## Reproduction and provenance

The frozen 80-image manifest is `appearance-holdout-frozen.json`. Its eight font families were absent from preceding vision experiments; each has five paired layouts/sizes. Images are synthetic, generated by `scripts/freeze-appearance-holdout.py` using local ImageMagick fonts. Fonts are not redistributed. SHA256 identifies each frozen PNG. `scripts/verify-appearance-guard.js` checks supplied image hashes before OCR. Initial projection and final component results are separate files; the final component change was motivated by known Arial/Trebuchet fixtures, not threshold calibration on held-out labels. A replacement design should receive a new untouched held-out set.

Run `python3 scripts/freeze-appearance-holdout.py`, then `GUARD_OUTPUT=evidence/new-local-guard-run.json node scripts/verify-appearance-guard.js`. Run `node scripts/verify-field-images.js` for full-label semantics. These scripts need ImageMagick and the named installed fonts. Generated images remain ignored; committed manifests and reports preserve identity. Historical evidence is unchanged.

## Remaining blocker

A new evidence-based design decision is required for reliable heading localization and font-weight verification. After that design passes the known and fresh held-out criteria, complete real-service accuracy/latency, 300-image browser validation, review, isolated Worker/preview verification, and production promotion with a recorded release tuple. Keep the present production deployment available until then.
