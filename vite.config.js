import {defineConfig} from 'vite';
// The review runs entirely in the browser: OCR, comparison and the local
// warning-heading weight measurement. No server route is required.
export default defineConfig({});
